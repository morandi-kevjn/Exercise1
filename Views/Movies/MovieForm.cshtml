<!-- created in test3 -->
@model Exercise1.ViewModels.MovieFormViewModels
@{
    ViewBag.Title = Model.Title;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@Model.Title</h2>
<!-- ep60 test4 -->
@Html.ValidationSummary(true, "Please fix the following errors.")

@using (Html.BeginForm("Save", "Movies"))
{
    // ep60 test4 old code <<<
    //<div class="form-group">
    @*Html.LabelFor(m => m.Movie.Name)*@
    @*Html.TextBoxFor(m => m.Movie.Name, new { @class = "form-control" })*@
    //</div>
    //<div class="form-group">
    @*Html.LabelFor(m => m.Movie.ReleaseDate)*@
    @*Html.TextBoxFor(m => m.Movie.ReleaseDate, "{0:d MMM yyyy}", new { @class = "form-control" })*@
    //</div>
    //<div class="form-group">
    @*Html.LabelFor(m => m.Movie.GenreId)*@
    @*Html.DropDownListFor(m => m.Movie.GenreId, new SelectList(Model.Genres, "Id", "Name"), "", new { @class = "form-control" })*@
    //</div>
    //<div class="form-group">
    @*Html.LabelFor(m => m.Movie.NumberInStock)*@
    @*Html.TextBoxFor(m => m.Movie.NumberInStock, new { @class = "form-control" })*@
    //</div>
    <!-- ep60 alternative to add Movie = new Movie() in the MoviesController-->
    @*Html.HiddenFor("Movie.Id", (Model.Movie != null ? Model.Movie.id : 0)*@
    @*Html.HiddenFor(m => m.Movie.Id)*@
    // >>>
    <div class="form-group">
        @Html.LabelFor(m => m.Name)
        @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
        <!-- Added ep60 test4 -->
        @Html.ValidationMessageFor(m => m.Name)
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.ReleaseDate)
        @Html.TextBoxFor(m => m.ReleaseDate, "{0:d MMM yyyy}", new { @class = "form-control" })
        <!-- Added ep60 test4 -->
        @Html.ValidationMessageFor(m => m.ReleaseDate)
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.GenreId)
        @Html.DropDownListFor(m => m.GenreId, new SelectList(Model.Genres, "Id", "Name"), "", new { @class = "form-control" })
        <!-- Added ep60 test4 -->
        @Html.ValidationMessageFor(m => m.GenreId)
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.NumberInStock)
        @Html.TextBoxFor(m => m.NumberInStock, new { @class = "form-control" })
        <!-- Added ep60 test4 -->
        @Html.ValidationMessageFor(m => m.NumberInStock)
    </div>
    @Html.HiddenFor(m => m.Id)
    @Html.AntiForgeryToken()
    <button type="submit" class="btn btn-primary">Save</button>
}
<!-- ep60 -->
@section scripts
{
    @Scripts.Render("~/bundles/jqueryval")
}
